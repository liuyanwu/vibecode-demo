diff --git a/utils/secureStorage/fallbackStorage.ts b/utils/secureStorage/fallbackStorage.ts
index 8667badd..24deb3bc 100644
--- a/utils/secureStorage/fallbackStorage.ts
+++ b/utils/secureStorage/fallbackStorage.ts
@@ -32,7 +32,7 @@ export function createFallbackStorage(
 
       if (result.success) {
         // Delete secondary when migrating to primary for the first time
-        // This preserves credentials when sharing .claude between host and containers
+        // This preserves credentials when sharing .vibecode between host and containers
         // See: https://github.com/anthropics/claude-code/issues/1414
         if (primaryDataBefore === null) {
           secondary.delete()
